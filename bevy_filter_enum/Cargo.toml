[package]
name = "bevy_filter_enum"
version = "0.5.0"
edition = "2021"
license = "MIT"
description = "Derive marker components from enum variants for Bevy ECS queries"

[dependencies]
bevy_app = "0.19.0-rc.1"
bevy_ecs = "0.19.0-rc.1"
bevy_filter_enum_derive = { version = "0.5.0", path = "../bevy_filter_enum_derive" }

[dev-dependencies]
strum_macros = "0.28"
